---
import Layout from '@layouts/Layout.astro'
//import * as data from '@data/home.json'
//import { Image } from 'astro:assets'

// const allProjects = data.projects
// const projects = allProjects

interface Props {
  pageTitle: string
  pageTLDR: string
  bodyClass: string
  mainClass: string
  ogImage?: string
}

const pageTitle = 'Architecture Photography'
const pageTLDR =
  'To compete on today markets, your property deserves HIGH QUALITY PHOTOGRAPHY'
const bodyClass = 'home'
const mainClass = 'no_padding'
---

<Layout
  bodyClass={bodyClass}
  pageTitle={pageTitle}
  mainClass={mainClass}
  pageTLDR={pageTLDR}
  ogImage="../og_home.jpg">
  <div class="container">
    <!-- {
      projects.map((project) => (
        <>
          <div>
            <small>
              <span>{project.building}</span>
              <span>{project.location}</span>
              <span>{project.country}</span> •
              <span>
                {project.architect}, <small>photo © Rocco Marosi</small>
              </span>
            </small>
            <br />
          </div>
        </>
      ))
    } -->
  </div>
</Layout>
